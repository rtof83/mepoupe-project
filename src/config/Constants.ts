export const config = {
	API_URL: 'http://localhost:5000',

	// Replace <user> and <password> with the DB user and password.
	// Replace myFirstDatabase with the name of the database that connections will use by default.
	MONGO_CONNECTION:
		'mongodb+srv://<user>:<password>@<database>.xxxxx.mongodb.net/myFirstDatabase?retryWrites=true&w=majority',
}
